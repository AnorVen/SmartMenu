# Roadmap SmartMenu

Этот документ фиксирует уже сделанные шаги и дальнейший план разработки. Формат специально оставлен как чеклист: новые задачи можно отмечать через `[x]`, а рядом с этапом заполнять статус, дату и заметки.

Легенда:

- `[x]` — сделано.
- `[ ]` — не сделано.
- `Статус` — `todo`, `in_progress`, `done`, `blocked`.

## Уже сделано

Статус: `done`  
Дата отметки: 2026-04-10  
Заметки: базовый каркас проекта готов.

- [x] Прочитаны исходные документы продукта: `Product Vision AI`, `Product vision MY`, `SmartMenu логика работы`.
- [x] Созданы Markdown-версии описания проекта в корне.
- [x] Выделены основные доменные сервисы из описания.
- [x] Добавлен отдельный `telegram-bot-gateway` как пользовательский вход.
- [x] Создан корневой Node.js workspace.
- [x] Созданы отдельные папки сервисов в `services/*`.
- [x] Для каждого сервиса добавлен `package.json`.
- [x] Для каждого сервиса добавлен `tsconfig.json` со строгой TypeScript-сборкой.
- [x] Для каждого сервиса добавлен Fastify HTTP-сервер.
- [x] Для каждого сервиса добавлен `/health`.
- [x] Для DB-сервисов добавлен `/health/db`.
- [x] Подготовлен `docker-compose.yml`.
- [x] Добавлен Postgres `16-alpine`.
- [x] Добавлен Redis `7-alpine`.
- [x] Созданы отдельные Postgres-базы для доменных сервисов.
- [x] Для каждого сервиса подготовлен Dockerfile.
- [x] Docker-образы всех сервисов успешно собирались.
- [x] Выбран Prisma как стандарт для схем и миграций.
- [x] Prisma подключен к DB-сервисам.
- [x] Добавлены `prisma/schema.prisma` для DB-сервисов.
- [x] Добавлены npm-скрипты `db:generate`, `db:migrate`, `db:deploy`, `db:push`.
- [x] Prisma Client генерируется при Docker-сборке.
- [x] Runtime-образы проверены на `npm audit --omit=dev` без уязвимостей на примере `user-profile-service`.
- [x] Расширен `telegram-bot-gateway` под реальные сценарии из описания проекта.
- [x] Бот реагирует на `/start`, `/profile`, `/generate`, `/route`, `/inventory`, `/summary`, `/help`.
- [x] Бот реагирует на покупку: `купила молоко 1 л`.
- [x] Бот реагирует на ручной ввод цены: `цена молоко 70 руб, Магнит`.
- [x] Бот реагирует на гостей: `в пятницу придут двое гостей`.
- [x] Бот реагирует на пожелание блюда: `хочу борщ в четверг`.
- [x] Бот реагирует на замену ингредиента: `замени курицу на тофу`.
- [x] Бот реагирует на приготовление блюда: `я приготовила ужин`.
- [x] Бот реагирует на пропуск приема пищи: `пропускаю ужин`.
- [x] Бот реагирует на незапланированный магазин: `я в магазине Пятерочка`.
- [x] Бот принимает геолокацию для маршрута.
- [x] Бот принимает фото чека как источник цен для будущего OCR.
- [x] Обновлена архитектурная документация.
- [x] Обновлен анализ языков: Go указан как практичный вариант для `inventory-manager` и `user-profile-service`.
- [x] Добавлена документация по структуре Git-репозиториев.
- [x] Создан корневой Git-репозиторий для документации, инфраструктуры и скриптов.
- [x] Созданы отдельные Git-репозитории для всех сервисов.
- [x] Корневой `.gitignore` настроен так, чтобы не трекать сервисы.
- [x] В сервисах добавлены отдельные `.gitignore`.
- [x] Добавлен `scripts/check-structure.mjs`.
- [x] Добавлен `scripts/sync-repositories.sh` для скачивания и обновления всех сервисных репозиториев.
- [x] Добавлен `scripts/bootstrap-full.sh` для полной локальной подготовки проекта.
- [x] В `package.json` добавлены команды `setup:repos`, `repos:sync`, `setup:full`, `bootstrap`.
- [x] Проверка структуры проходит.

## Цель ближайшего MVP

Статус: `in_progress`  
Дата начала: 2026-04-10  
Дата завершения:  
Заметки: профиль уже сохраняется в Postgres через `user-profile-service`, а `inventory-manager` уже добавляет и списывает продукты.

Собрать работающий сценарий:

- [x] Пользователь пишет `/start`.
- [x] Бот собирает профиль.
- [x] `user-profile-service` сохраняет профиль в Postgres.
- [x] Пользователь пишет `/generate`.
- [x] `ai-orchestrator` получает профиль, остатки, рецепты и цены.
- [x] Система возвращает меню, список покупок и маршрут.
- [x] Пользователь пишет `купила ...`.
- [x] `inventory-manager` добавляет продукты в инвентарь.
- [x] Пользователь пишет `я приготовила ...`.
- [x] `inventory-manager` списывает продукты.
- [x] Пользователь пишет `пропускаю ужин`.
- [x] `ai-orchestrator` перепланирует оставшиеся блюда.

## Этап 1. API-контракты и валидация

Статус: `in_progress`  
Дата начала: 2026-04-10  
Дата завершения:  
Заметки: Zod-валидация добавлена в `user-profile-service` и `inventory-manager`, остальные сервисы еще ждут своей очереди.

Цель: перестать передавать между сервисами произвольный `rawText` там, где уже понятна структура данных.

Задачи:

- [ ] Добавить Zod во все сервисы.
- [x] Описать DTO для `user-profile-service`.
- [x] Ввести единый формат ошибок в `user-profile-service`.
- [x] Ввести `chatId` как обязательный идентификатор пользователя в `user-profile-service`.
- [ ] Добавить `requestId` или `correlationId` для трассировки.
- [ ] Добавить минимальные OpenAPI-описания для публичных endpoint-ов сервисов.

Приоритетные сервисы:

- [ ] `telegram-bot-gateway`
- [x] `user-profile-service`
- [x] `inventory-manager`
- [x] `ai-orchestrator`

## Этап 2. Реальный `user-profile-service`

Статус: `done`  
Дата начала: 2026-04-10  
Дата завершения: 2026-04-10  
Заметки: реализованы Prisma CRUD, Zod-валидация и smoke-тест с реальным Postgres.

Цель: бот реально сохраняет профиль пользователя, семью, ограничения и гостей в Postgres.

Endpoint-ы:

- [x] `POST /profiles` — создать или обновить профиль по `chatId`.
- [x] `GET /constraints?chatId=...` — вернуть ограничения для генерации меню.
- [x] `POST /guests` — сохранить гостей на конкретный день и прием пищи.
- [x] `GET /profiles/:chatId` — получить полный профиль для отладки.

Логика:

- [x] Создавать профиль с дефолтами, если пользователя нет.
- [x] Хранить бюджет, вес, транспорт, технику, диету, аллергии, нелюбимые продукты.
- [x] Хранить семейные особенности и household.
- [x] Хранить расписание гостей.
- [x] Возвращать оркестратору только поля, нужные для генерации.

Проверки:

- [ ] Unit-тесты на маппинг данных из Telegram-анкеты в Prisma.
- [x] Smoke-тест `POST /profiles → GET /constraints → POST /guests → GET /profiles/:chatId` с реальным Postgres.

## Этап 3. Реальный `inventory-manager`

Статус: `done`  
Дата начала: 2026-04-10  
Дата завершения: 2026-04-10  
Заметки: реализованы Prisma CRUD, Zod-валидация, парсинг покупок и расхода, а также живой smoke-тест через Docker и Postgres.

Цель: продукты после покупки попадают в “холодильник”, а после готовки списываются.

Endpoint-ы:

- [x] `POST /items` - добавить продукты после покупки.
- [x] `GET /remaining?chatId=...` - получить активные остатки.
- [x] `POST /consume` - списать ингредиенты после готовки.
- [x] `GET /expiring?chatId=...` - получить продукты, которые скоро испортятся.
- [x] `POST /cleanup-expired` - служебный endpoint или worker-задача для очистки просрочки.

Логика:

- [x] Парсить простые строки вида `купила молоко 1 л, курица 500 г`.
- [x] Определять категорию продукта.
- [x] Назначать срок годности по категории.
- [x] При списании выбирать продукты с ближайшим сроком годности.
- [x] В MVP списывать продукт целиком.
- [ ] Позже добавить частичное списание: 150 г из 200 г.

Проверки:

- [x] Добавление покупки.
- [x] Получение остатков.
- [x] Списание после готовки.
- [x] Приоритет списания по ближайшему сроку годности.

## Этап 4. `recipe-content-service` и seed-рецепты

Статус: `done`  
Дата начала: 2026-04-11  
Дата завершения: 2026-04-11  
Заметки: добавлены seed-рецепты, поиск по остаткам, базовые замены ингредиентов и живой smoke-тест через Docker и Postgres.

Цель: оркестратор получает не пустые заглушки, а реальный список простых рецептов.

Endpoint-ы:

- [x] `GET /recipes`
- [x] `GET /recipes/:id`
- [x] `POST /recipes/search-by-leftovers`
- [x] `POST /substitutions`

Seed-рецепты для MVP:

- [x] овсянка с молоком;
- [x] гречка с курицей;
- [x] рис с овощами;
- [x] омлет с зеленью;
- [x] куриный суп;
- [x] паста с овощами;
- [x] быстрые сэндвичи;
- [x] салат с курицей;
- [x] творог/йогурт с фруктами;
- [x] тушеные овощи на сковороде.

Фильтры:

- [x] без духовки;
- [x] время готовки;
- [ ] аллергии;
- [x] диета;
- [x] совпадение по остаткам.

Проверки:

- [x] Поиск рецептов по остаткам.
- [x] Исключение рецептов с духовкой.
- [x] Замена ингредиента.

## Этап 5. MVP `retail-data-aggregator`

Статус: `done`  
Дата начала: 2026-04-11  
Дата завершения: 2026-04-11  
Заметки: реализованы ручные цены, агрегированные медианы, fallback-цены, список магазинов и сравнение shopping list. Живой smoke-тест прошел через локальный Postgres и HTTP API.

Цель: цены и магазины начинают участвовать в меню и маршрутах.

Endpoint-ы:

- [x] `GET /prices?product=...&chain=...`
- [x] `POST /prices/manual`
- [x] `POST /compare`
- [x] `GET /stores`

Логика:

- [x] Сохранять ручной ввод цены: `цена молоко 70 руб, Магнит`.
- [x] Считать агрегированную цену по медиане.
- [x] Возвращать fallback-цены, если реальных данных нет.
- [x] Хранить базовые магазины с координатами.
- [ ] Кэшировать частые запросы в Redis.

Проверки:

- [x] Сохранение ручной цены.
- [x] Пересчет медианы.
- [x] Сравнение цен для списка покупок.

## Этап 6. MVP `ai-orchestrator` без настоящего LLM

Статус: `in_progress`  
Дата начала: 2026-04-11  
Дата завершения:  
Заметки: базовая детерминированная генерация работает, осталось добавить кэш текущего меню в Redis.

Цель: получить работающую генерацию меню до подключения LLM.

Почему сначала без LLM:

- [x] проще отладить контракты;
- [x] легче тестировать бюджет, вес и zero-waste;
- [x] не тратятся деньги на API;
- [x] можно подключить LLM позже как adapter.

Endpoint-ы:

- [x] `POST /generate`
- [x] `POST /meal-skipped`
- [x] `GET /menus/current?chatId=...`
- [x] `GET /menus/:id/shopping-list`

Логика:

- [x] Получить ограничения из `user-profile-service`.
- [x] Получить остатки из `inventory-manager`.
- [x] Получить рецепты из `recipe-content-service`.
- [x] Получить цены из `retail-data-aggregator`.
- [x] Собрать меню на неделю.
- [x] Проверить бюджет.
- [x] Проверить вес покупок.
- [x] Использовать скоропортящиеся продукты раньше.
- [x] Сохранить меню в Postgres.
- [ ] Кэшировать текущее меню в Redis.

Проверки:

- [x] Генерация меню для одного пользователя.
- [x] Учет отсутствия духовки.
- [x] Учет семьи.
- [x] Учет гостей.
- [x] Перепланирование после пропуска ужина.

## Этап 7. `logistics-route-planner`

Статус: `in_progress`  
Дата начала: 2026-04-11  
Дата завершения:  
Заметки: реализованы построение маршрута по текущему shopping list, сохранение RoutePlan, контроль веса и сценарий неожиданного магазина. Пока нет разбиения покупок на отдельные дни и нет учета выхода из магазина.

Цель: маршрут покупок перестает быть текстовой заглушкой.

Endpoint-ы:

- [x] `POST /routes`
- [x] `POST /weight-check`
- [x] `GET /routes/current?chatId=...`
- [x] `POST /routes/unexpected-store`

Логика:

- [x] Получить список покупок.
- [x] Сгруппировать товары по магазинам.
- [x] Проверить вес на один поход.
- [ ] Разбить покупки на несколько дней, если вес превышен.
- [x] Построить простой маршрут по ближайшим магазинам.
- [x] В MVP считать расстояние по прямой.
- [ ] Позже подключить Maps API.

Гео-сценарии:

- [x] пользователь рядом с магазином;
- [x] пользователь зашел в незапланированный магазин;
- [ ] пользователь вышел из магазина;
- [ ] предложение купить будущий товар дешевле.

## Этап 8. Реальный Telegram UX

Статус: `in_progress`  
Дата начала: 2026-04-10  
Дата завершения:  
Заметки: базовые команды и фразовые сценарии уже добавлены, но UX еще не полноценный.

Задачи:

- [x] Добавить inline-клавиатуры для главных сценариев.
- [ ] Добавить команды Telegram через `setMyCommands`.
- [ ] Добавить нормальные ошибки и подсказки.
- [ ] Добавить подтверждение перед изменением меню.
- [x] Добавить сценарий “гости” с уточнением предпочтений гостей.
- [x] Добавить сценарий “замена ингредиента”.
- [x] Добавить сценарий “я приготовил(а)”.
- [ ] Добавить сценарий “купил не все” после магазина.
- [ ] Добавить выбор конкретного блюда при отметке “я приготовил(а)”.
- [ ] Добавить выбор конкретного приема пищи при пропуске.

## Этап 9. Подключение LLM

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: заменить часть детерминированной генерации на AI, но сохранить валидацию.

Задачи:

- [ ] Вынести LLM в adapter внутри `ai-orchestrator`.
- [ ] Описать JSON Schema ответа LLM.
- [ ] Валидировать ответ через Zod.
- [ ] Делать максимум 2 повтора при невалидном JSON.
- [ ] Проверять бюджет и вес после ответа LLM.
- [ ] Иметь fallback-меню без LLM.
- [ ] Логировать промпты и ответы без персональных данных.

Сценарии LLM:

- [ ] генерация меню;
- [ ] перепланирование после пропуска;
- [ ] адаптация рецепта;
- [ ] объяснение замены ингредиента;
- [ ] краткие подсказки для новичка.

## Этап 10. Workers и расписания

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: фоновые задачи работают без ручных запросов пользователя.

Задачи:

- [ ] Worker для истекающих продуктов.
- [ ] Worker для очистки просрочки.
- [ ] Worker для недельной сводки.
- [ ] Worker для пересчета агрегированных цен.

Варианты:

- [ ] отдельный process внутри сервиса;
- [ ] отдельный container per worker;
- [ ] позже очередь событий.

Для MVP проще отдельный worker-process в соответствующем сервисе.

## Этап 11. Событийная архитектура

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: приблизиться к идеальному сценарию из описания проекта.

События:

- [ ] `InventoryChanged`
- [ ] `MealSkipped`
- [ ] `MealCooked`
- [ ] `UserLocationChanged`
- [ ] `GuestPlanned`
- [ ] `PriceUpdated`
- [ ] `RouteChanged`

Варианты брокера:

- [ ] RabbitMQ;
- [ ] NATS;
- [ ] Kafka только при большой аналитической нагрузке.

Для MVP брокер не нужен. Синхронный REST проще и быстрее.

## Этап 12. Production-подготовка

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Задачи:

- [ ] Auth между сервисами.
- [ ] Rate limiting.
- [ ] Structured logging.
- [ ] Metrics.
- [ ] Health checks для БД и Redis.
- [ ] CI: lint, build, test, docker build.
- [ ] Prisma migrations в CI/CD.
- [ ] Secrets через env/secret manager.
- [ ] Backup Postgres.
- [ ] Разделение staging/prod.

## Рекомендуемый порядок прямо сейчас

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

1. [x] Реализовать `user-profile-service`.
2. [x] Реализовать `inventory-manager`.
3. [x] Добавить seed-рецепты и `recipe-content-service`.
4. [x] Сделать детерминированный `ai-orchestrator`.
5. [x] Подключить `retail-data-aggregator`.
6. [x] Реализовать простой `logistics-route-planner`.
7. [ ] Улучшить Telegram UX.
8. [ ] Только потом подключать LLM.

Самый правильный ближайший коммит: улучшение Telegram UX и доведение `ai-orchestrator` до Redis-кэша текущего меню.
