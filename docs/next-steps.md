# Roadmap SmartMenu

Этот документ фиксирует уже сделанные шаги и дальнейший план разработки. Формат специально оставлен как чеклист: новые задачи можно отмечать через `[x]`, а рядом с этапом заполнять статус, дату и заметки.

Легенда:

- `[x]` — сделано.
- `[ ]` — не сделано.
- `Статус` — `todo`, `in_progress`, `done`, `blocked`.

## Уже сделано

Статус: `done`  
Дата отметки: 2026-04-10  
Заметки: базовый каркас проекта готов.

- [x] Прочитаны исходные документы продукта: `Product Vision AI`, `Product vision MY`, `SmartMenu логика работы`.
- [x] Созданы Markdown-версии описания проекта в корне.
- [x] Выделены основные доменные сервисы из описания.
- [x] Добавлен отдельный `telegram-bot-gateway` как пользовательский вход.
- [x] Создан корневой Node.js workspace.
- [x] Созданы отдельные папки сервисов в `services/*`.
- [x] Для каждого сервиса добавлен `package.json`.
- [x] Для каждого сервиса добавлен `tsconfig.json` со строгой TypeScript-сборкой.
- [x] Для каждого сервиса добавлен Fastify HTTP-сервер.
- [x] Для каждого сервиса добавлен `/health`.
- [x] Для DB-сервисов добавлен `/health/db`.
- [x] Подготовлен `docker-compose.yml`.
- [x] Добавлен Postgres `16-alpine`.
- [x] Добавлен Redis `7-alpine`.
- [x] Созданы отдельные Postgres-базы для доменных сервисов.
- [x] Для каждого сервиса подготовлен Dockerfile.
- [x] Docker-образы всех сервисов успешно собирались.
- [x] Выбран Prisma как стандарт для схем и миграций.
- [x] Prisma подключен к DB-сервисам.
- [x] Добавлены `prisma/schema.prisma` для DB-сервисов.
- [x] Добавлены npm-скрипты `db:generate`, `db:migrate`, `db:deploy`, `db:push`.
- [x] Prisma Client генерируется при Docker-сборке.
- [x] Runtime-образы проверены на `npm audit --omit=dev` без уязвимостей на примере `user-profile-service`.
- [x] Расширен `telegram-bot-gateway` под реальные сценарии из описания проекта.
- [x] Бот реагирует на `/start`, `/profile`, `/generate`, `/route`, `/inventory`, `/summary`, `/help`.
- [x] Бот реагирует на покупку: `купила молоко 1 л`.
- [x] Бот реагирует на ручной ввод цены: `цена молоко 70 руб, Магнит`.
- [x] Бот реагирует на гостей: `в пятницу придут двое гостей`.
- [x] Бот реагирует на пожелание блюда: `хочу борщ в четверг`.
- [x] Бот реагирует на замену ингредиента: `замени курицу на тофу`.
- [x] Бот реагирует на приготовление блюда: `я приготовила ужин`.
- [x] Бот реагирует на пропуск приема пищи: `пропускаю ужин`.
- [x] Бот реагирует на незапланированный магазин: `я в магазине Пятерочка`.
- [x] Бот принимает геолокацию для маршрута.
- [x] Бот принимает фото чека как источник цен для будущего OCR.
- [x] Обновлена архитектурная документация.
- [x] Обновлен анализ языков: Go указан как практичный вариант для `inventory-manager` и `user-profile-service`.
- [x] Добавлена документация по структуре Git-репозиториев.
- [x] Создан корневой Git-репозиторий для документации, инфраструктуры и скриптов.
- [x] Созданы отдельные Git-репозитории для всех сервисов.
- [x] Корневой `.gitignore` настроен так, чтобы не трекать сервисы.
- [x] В сервисах добавлены отдельные `.gitignore`.
- [x] Добавлен `scripts/check-structure.mjs`.
- [x] Добавлен `scripts/sync-repositories.sh` для скачивания и обновления всех сервисных репозиториев.
- [x] Добавлен `scripts/bootstrap-full.sh` для полной локальной подготовки проекта.
- [x] В `package.json` добавлены команды `setup:repos`, `repos:sync`, `setup:full`, `bootstrap`.
- [x] Проверка структуры проходит.

## Цель ближайшего MVP

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Собрать работающий сценарий:

- [ ] Пользователь пишет `/start`.
- [ ] Бот собирает профиль.
- [ ] `user-profile-service` сохраняет профиль в Postgres.
- [ ] Пользователь пишет `/generate`.
- [ ] `ai-orchestrator` получает профиль, остатки, рецепты и цены.
- [ ] Система возвращает меню, список покупок и маршрут.
- [ ] Пользователь пишет `купила ...`.
- [ ] `inventory-manager` добавляет продукты в инвентарь.
- [ ] Пользователь пишет `я приготовила ...`.
- [ ] `inventory-manager` списывает продукты.
- [ ] Пользователь пишет `пропускаю ужин`.
- [ ] `ai-orchestrator` перепланирует оставшиеся блюда.

## Этап 1. API-контракты и валидация

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: перестать передавать между сервисами произвольный `rawText` там, где уже понятна структура данных.

Задачи:

- [ ] Добавить Zod в сервисы.
- [ ] Описать DTO для входящих запросов и ответов.
- [ ] Ввести единый формат ошибок.
- [ ] Ввести `chatId` как обязательный идентификатор пользователя в MVP.
- [ ] Добавить `requestId` или `correlationId` для трассировки.
- [ ] Добавить минимальные OpenAPI-описания для публичных endpoint-ов сервисов.

Приоритетные сервисы:

- [ ] `telegram-bot-gateway`
- [ ] `user-profile-service`
- [ ] `inventory-manager`
- [ ] `ai-orchestrator`

## Этап 2. Реальный `user-profile-service`

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: бот реально сохраняет профиль пользователя, семью, ограничения и гостей в Postgres.

Endpoint-ы:

- [ ] `POST /profiles` — создать или обновить профиль по `chatId`.
- [ ] `GET /constraints?chatId=...` — вернуть ограничения для генерации меню.
- [ ] `POST /guests` — сохранить гостей на конкретный день и прием пищи.
- [ ] `GET /profiles/:chatId` — получить полный профиль для отладки.

Логика:

- [ ] Создавать профиль с дефолтами, если пользователя нет.
- [ ] Хранить бюджет, вес, транспорт, технику, диету, аллергии, нелюбимые продукты.
- [ ] Хранить семейные особенности и участников семьи.
- [ ] Хранить расписание гостей.
- [ ] Возвращать оркестратору только поля, нужные для генерации.

Проверки:

- [ ] Unit-тесты на маппинг данных из Telegram-анкеты в Prisma.
- [ ] Интеграционный тест `POST /profiles → GET /constraints`.

## Этап 3. Реальный `inventory-manager`

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: продукты после покупки попадают в “холодильник”, а после готовки списываются.

Endpoint-ы:

- [ ] `POST /items` — добавить продукты после покупки.
- [ ] `GET /remaining?chatId=...` — получить активные остатки.
- [ ] `POST /consume` — списать ингредиенты после готовки.
- [ ] `GET /expiring?chatId=...` — получить продукты, которые скоро испортятся.
- [ ] `POST /cleanup-expired` — служебный endpoint или worker-задача для очистки просрочки.

Логика:

- [ ] Парсить простые строки вида `купила молоко 1 л, курица 500 г`.
- [ ] Определять категорию продукта.
- [ ] Назначать срок годности по категории.
- [ ] При списании выбирать продукты с ближайшим сроком годности.
- [ ] В MVP списывать продукт целиком.
- [ ] Позже добавить частичное списание: 150 г из 200 г.

Проверки:

- [ ] Добавление покупки.
- [ ] Получение остатков.
- [ ] Списание после готовки.
- [ ] Приоритет списания по ближайшему сроку годности.

## Этап 4. `recipe-content-service` и seed-рецепты

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: оркестратор получает не пустые заглушки, а реальный список простых рецептов.

Endpoint-ы:

- [ ] `GET /recipes`
- [ ] `GET /recipes/:id`
- [ ] `POST /recipes/search-by-leftovers`
- [ ] `POST /substitutions`

Seed-рецепты для MVP:

- [ ] овсянка с молоком;
- [ ] гречка с курицей;
- [ ] рис с овощами;
- [ ] омлет с зеленью;
- [ ] куриный суп;
- [ ] паста с овощами;
- [ ] быстрые сэндвичи;
- [ ] салат с курицей;
- [ ] творог/йогурт с фруктами;
- [ ] тушеные овощи на сковороде.

Фильтры:

- [ ] без духовки;
- [ ] время готовки;
- [ ] аллергии;
- [ ] диета;
- [ ] совпадение по остаткам.

Проверки:

- [ ] Поиск рецептов по остаткам.
- [ ] Исключение рецептов с духовкой.
- [ ] Замена ингредиента.

## Этап 5. MVP `retail-data-aggregator`

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: цены и магазины начинают участвовать в меню и маршрутах.

Endpoint-ы:

- [ ] `GET /prices?product=...&chain=...`
- [ ] `POST /prices/manual`
- [ ] `POST /compare`
- [ ] `GET /stores`

Логика:

- [ ] Сохранять ручной ввод цены: `цена молоко 70 руб, Магнит`.
- [ ] Считать агрегированную цену по медиане.
- [ ] Возвращать fallback-цены, если реальных данных нет.
- [ ] Хранить базовые магазины с координатами.
- [ ] Кэшировать частые запросы в Redis.

Проверки:

- [ ] Сохранение ручной цены.
- [ ] Пересчет медианы.
- [ ] Сравнение цен для списка покупок.

## Этап 6. MVP `ai-orchestrator` без настоящего LLM

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: получить работающую генерацию меню до подключения LLM.

Почему сначала без LLM:

- [ ] проще отладить контракты;
- [ ] легче тестировать бюджет, вес и zero-waste;
- [ ] не тратятся деньги на API;
- [ ] можно подключить LLM позже как adapter.

Endpoint-ы:

- [ ] `POST /generate`
- [ ] `POST /meal-skipped`
- [ ] `GET /menus/current?chatId=...`
- [ ] `GET /menus/:id/shopping-list`

Логика:

- [ ] Получить ограничения из `user-profile-service`.
- [ ] Получить остатки из `inventory-manager`.
- [ ] Получить рецепты из `recipe-content-service`.
- [ ] Получить цены из `retail-data-aggregator`.
- [ ] Собрать меню на неделю.
- [ ] Проверить бюджет.
- [ ] Проверить вес покупок.
- [ ] Использовать скоропортящиеся продукты раньше.
- [ ] Сохранить меню в Postgres.
- [ ] Кэшировать текущее меню в Redis.

Проверки:

- [ ] Генерация меню для одного пользователя.
- [ ] Учет отсутствия духовки.
- [ ] Учет семьи.
- [ ] Учет гостей.
- [ ] Перепланирование после пропуска ужина.

## Этап 7. `logistics-route-planner`

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: маршрут покупок перестает быть текстовой заглушкой.

Endpoint-ы:

- [ ] `POST /routes`
- [ ] `POST /weight-check`
- [ ] `GET /routes/current?chatId=...`
- [ ] `POST /routes/unexpected-store`

Логика:

- [ ] Получить список покупок.
- [ ] Сгруппировать товары по магазинам.
- [ ] Проверить вес на один поход.
- [ ] Разбить покупки на несколько дней, если вес превышен.
- [ ] Построить простой маршрут по ближайшим магазинам.
- [ ] В MVP считать расстояние по прямой.
- [ ] Позже подключить Maps API.

Гео-сценарии:

- [ ] пользователь рядом с магазином;
- [ ] пользователь зашел в незапланированный магазин;
- [ ] пользователь вышел из магазина;
- [ ] предложение купить будущий товар дешевле.

## Этап 8. Реальный Telegram UX

Статус: `in_progress`  
Дата начала: 2026-04-10  
Дата завершения:  
Заметки: базовые команды и фразовые сценарии уже добавлены, но UX еще не полноценный.

Задачи:

- [x] Добавить inline-клавиатуры для главных сценариев.
- [ ] Добавить команды Telegram через `setMyCommands`.
- [ ] Добавить нормальные ошибки и подсказки.
- [ ] Добавить подтверждение перед изменением меню.
- [x] Добавить сценарий “гости” с уточнением предпочтений гостей.
- [x] Добавить сценарий “замена ингредиента”.
- [x] Добавить сценарий “я приготовил(а)”.
- [ ] Добавить сценарий “купил не все” после магазина.
- [ ] Добавить выбор конкретного блюда при отметке “я приготовил(а)”.
- [ ] Добавить выбор конкретного приема пищи при пропуске.

## Этап 9. Подключение LLM

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: заменить часть детерминированной генерации на AI, но сохранить валидацию.

Задачи:

- [ ] Вынести LLM в adapter внутри `ai-orchestrator`.
- [ ] Описать JSON Schema ответа LLM.
- [ ] Валидировать ответ через Zod.
- [ ] Делать максимум 2 повтора при невалидном JSON.
- [ ] Проверять бюджет и вес после ответа LLM.
- [ ] Иметь fallback-меню без LLM.
- [ ] Логировать промпты и ответы без персональных данных.

Сценарии LLM:

- [ ] генерация меню;
- [ ] перепланирование после пропуска;
- [ ] адаптация рецепта;
- [ ] объяснение замены ингредиента;
- [ ] краткие подсказки для новичка.

## Этап 10. Workers и расписания

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: фоновые задачи работают без ручных запросов пользователя.

Задачи:

- [ ] Worker для истекающих продуктов.
- [ ] Worker для очистки просрочки.
- [ ] Worker для недельной сводки.
- [ ] Worker для пересчета агрегированных цен.

Варианты:

- [ ] отдельный process внутри сервиса;
- [ ] отдельный container per worker;
- [ ] позже очередь событий.

Для MVP проще отдельный worker-process в соответствующем сервисе.

## Этап 11. Событийная архитектура

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Цель: приблизиться к идеальному сценарию из описания проекта.

События:

- [ ] `InventoryChanged`
- [ ] `MealSkipped`
- [ ] `MealCooked`
- [ ] `UserLocationChanged`
- [ ] `GuestPlanned`
- [ ] `PriceUpdated`
- [ ] `RouteChanged`

Варианты брокера:

- [ ] RabbitMQ;
- [ ] NATS;
- [ ] Kafka только при большой аналитической нагрузке.

Для MVP брокер не нужен. Синхронный REST проще и быстрее.

## Этап 12. Production-подготовка

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

Задачи:

- [ ] Auth между сервисами.
- [ ] Rate limiting.
- [ ] Structured logging.
- [ ] Metrics.
- [ ] Health checks для БД и Redis.
- [ ] CI: lint, build, test, docker build.
- [ ] Prisma migrations в CI/CD.
- [ ] Secrets через env/secret manager.
- [ ] Backup Postgres.
- [ ] Разделение staging/prod.

## Рекомендуемый порядок прямо сейчас

Статус: `todo`  
Дата начала:  
Дата завершения:  
Заметки:

1. [ ] Реализовать `user-profile-service`.
2. [ ] Реализовать `inventory-manager`.
3. [ ] Добавить seed-рецепты и `recipe-content-service`.
4. [ ] Сделать детерминированный `ai-orchestrator`.
5. [ ] Подключить `retail-data-aggregator`.
6. [ ] Реализовать простой `logistics-route-planner`.
7. [ ] Улучшить Telegram UX.
8. [ ] Только потом подключать LLM.

Самый правильный ближайший коммит: `user-profile-service` с Prisma CRUD, Zod-валидацией и тестом `POST /profiles → GET /constraints`.
